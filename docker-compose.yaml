services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        UID: ${DOCKER_UID:-1000}
        GID: ${DOCKER_GID:-1000}
    image: go-memory-db
    container_name: go-memory-db-dev
    volumes:
      - .:/app
    working_dir: /app
    ports:
      - "6379:6379"
